{
  "private": true,
  "description": "The :cljs host verification. json-ld-api is .cljc and to-rdf formats numbers per host into strings that get SIGNED, so both branches must agree. The classpath is derived from deps.edn via `clojure -Spath` rather than written as sibling paths: sibling checkouts exist in the west workspace but not in CI.",
  "scripts": {
    "smoke": "nbb --classpath \"$(clojure -Spath | tr ':' '\\n' | grep -E '(^src$|gitlibs)' | tr '\\n' ':')\" test/nbb_smoke.cljs"
  }
}
